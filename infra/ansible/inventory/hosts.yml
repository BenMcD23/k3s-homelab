---
all:
  vars:
    tailnet: tail02e471.ts.net
  children:
    amd64:
      hosts:
        home:
          ansible_host: home-server.{{ tailnet }}
          ansible_user: ben
          node_role: interactive
        squadron:
          ansible_host: 317server.{{ tailnet }}
          ansible_user: server317
          node_role: batch
    arm64:
      hosts:
        oracle:
          ansible_host: k8s-node.{{ tailnet }}
          ansible_user: ubuntu
          node_role: edge
